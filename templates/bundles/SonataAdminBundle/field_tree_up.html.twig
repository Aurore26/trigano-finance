{% extends 'SonataAdminBundle:CRUD:base_list_field.html.twig' %}

{% block field %}

    {% spaceless %}
        {% if object.parent %}
			{% if object.parent.children[0].id != object.id %}
            <a href="{{ path('categorie_tree_up', {'categorie_id': object.id}) }}">
                ^
            </a>
        	{% endif %}
        {% endif %}
    {% endspaceless %}
{% endblock %}